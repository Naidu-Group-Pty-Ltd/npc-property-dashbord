name: npc_api
description: Scope-restricted edge-function client for the NPC platform.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  http: ^1.2.0
  npc_core: ^0.1.0
dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
