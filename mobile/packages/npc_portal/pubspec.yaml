name: npc_portal
description: Portal descriptors, session transport and the shell shared by all five NPC apps.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  http: ^1.2.0
  meta: ^1.16.0
  npc_api: ^0.1.0
  npc_auth: ^0.1.0
  npc_brand: ^0.1.0
  npc_core: ^0.1.0
  npc_design_system: ^0.1.0
  npc_tenant: ^0.1.0
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
