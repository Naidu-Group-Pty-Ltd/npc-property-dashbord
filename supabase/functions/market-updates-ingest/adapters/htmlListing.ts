import { DOMParser } from 'npm:linkedom@0.18.12'; import type { MarketSourceAdapter,NormalisedSourceBatch,NormalisedSourceItem,SourceConfig,SourceValidationResult } from './types.ts'; import { boundedFetch,normaliseUrl,sourceDomains } from './security.ts';
export class HtmlListingAdapter implements MarketSourceAdapter { async read(source:SourceConfig,url:string){const allowed=sourceDomains(source);const {response,body,latency}=await boundedFetch(url,allowed,{headers:{accept:'text/html','user-agent':Deno.env.get('MARKET_UPDATES_USER_AGENT')||'Aurixa-Market-Intelligence/2.0'}});const doc=new DOMParser().parseFromString(body,'text/html');const out:NormalisedSourceItem[]=[];for(const node of [...doc.querySelectorAll('script[type="application/ld+json"]')]){try{const raw=JSON.parse(node.textContent||'null');for(const x of (Array.isArray(raw)?raw:[raw]).flatMap((v:any)=>v?.['@graph']||[v]))if(['Article','NewsArticle','Report'].includes(x?.['@type'])){const canonical=normaliseUrl(x.url||x.mainEntityOfPage?.['@id'],url,allowed);out.push({externalId:x.identifier?.value||x.identifier||canonical,title:x.headline||x.name,canonicalUrl:canonical,originalUrl:x.url||canonical,publishedAt:x.datePublished&&Date.parse(x.datePublished)?new Date(x.datePublished).toISOString():null,excerpt:x.description||null,author:x.author?.name||null,category:x.articleSection||null});}}catch{/* malformed public metadata is ignored */}}
const cfg=source.adapter_config as {item_selector?:string;title_selector?:string;link_selector?:string;date_selector?:string;excerpt_selector?:string};for(const item of [...doc.querySelectorAll(cfg.item_selector||'article')]){try{const link=item.querySelector(cfg.link_selector||'a[href]') as HTMLAnchorElement|null;const title=(item.querySelector(cfg.title_selector||'h2,h3')?.textContent||link?.textContent||'').trim();if(!link||!title)continue;const canonical=normaliseUrl(link.href,url,allowed);const dt=(item.querySelector(cfg.date_selector||'time') as HTMLTimeElement|null)?.dateTime;out.push({externalId:canonical,title,canonicalUrl:canonical,originalUrl:link.href,publishedAt:dt&&Date.parse(dt)?new Date(dt).toISOString():null,excerpt:item.querySelector(cfg.excerpt_selector||'p')?.textContent?.trim()||null,author:null,category:null});}catch{/* reject unsafe/malformed item */}}
const unique=[...new Map(out.map(x=>[x.canonicalUrl,x])).values()].slice(0,Number(Deno.env.get('MARKET_UPDATES_MAX_ITEMS_PER_SOURCE')||40));if(!unique.length)throw new Error('Listing layout yielded no public article metadata');return{items:unique,validation:{valid:true,format:'html_listing',itemCount:unique.length,endpoint:url,httpStatus:response.status,latencyMs:latency}} as NormalisedSourceBatch} async validate(s:SourceConfig):Promise<SourceValidationResult>{try{return(await this.read(s,s.listing_urls[0]||s.primary_url!)).validation}catch(e){return{valid:false,format:'html_listing',itemCount:0,safeError:String((e as Error).message).slice(0,240)}}} async fetch(s:SourceConfig){let last:Error|undefined;for(const u of s.listing_urls.length?s.listing_urls:[s.primary_url!])try{return await this.read(s,u)}catch(e){last=e as Error}throw last||new Error('No listing configured')}}
