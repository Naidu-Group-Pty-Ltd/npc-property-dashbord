import type { MarketSourceAdapter,SourceConfig } from './types.ts'; import {RssAtomAdapter} from './rssAtom.ts'; import {HtmlListingAdapter} from './htmlListing.ts';
class UnavailableAdapter implements MarketSourceAdapter{constructor(private reason:string){}async validate(){return{valid:false,format:'manual',itemCount:0,safeError:this.reason}}async fetch(){throw new Error(this.reason)}}
export class FederalLegislationApiAdapter extends UnavailableAdapter{constructor(){super('Federal Register API adapter requires documented OData resource configuration from adapter_config')}} export class LicensedPartnerAdapter extends UnavailableAdapter{constructor(){super('Licensed partner credentials are not configured')}} export class ManualAdapter extends UnavailableAdapter{constructor(){super('Manual sources are not fetched automatically')}}
export function adapterFor(s:SourceConfig):MarketSourceAdapter{if(['rss','atom','rss_multi'].includes(s.adapter_type))return new RssAtomAdapter();if(['html_listing'].includes(s.adapter_type))return new HtmlListingAdapter();if(s.adapter_type==='official_api')return new FederalLegislationApiAdapter();if(['licensed_partner_feed'].includes(s.adapter_type))return new LicensedPartnerAdapter();return new ManualAdapter()}
export {RssAtomAdapter,HtmlListingAdapter}; export type * from './types.ts';
