import { Inbox } from 'lucide-react';
import { Card, CardContent } from '@/components/ui/card';
import { SolicitorPortalShell } from '@/components/solicitor-portal/SolicitorPortalShell';
export default function SolicitorWorkspacePage({kind}:{kind:'messages'|'tasks'|'notifications'|'settings'}){const copy={messages:['Messages','Case conversations appear when you are added as a participant.'],tasks:['Tasks','Shared and legal tasks appear when a matter runway is configured.'],notifications:['Notifications','Updates appear when cases, access, documents, or projections change.'],settings:['Settings','Manage your security and portal preferences.']}[kind];return <SolicitorPortalShell title={copy[0]} description={copy[1]}><Card><CardContent className="py-14 text-center"><Inbox className="mx-auto h-8 w-8 text-muted-foreground" aria-hidden/><p className="mt-3 font-medium">Nothing needs your attention</p><p className="mt-1 text-sm text-muted-foreground">Open a matter or ask your practice administrator to confirm your case access.</p></CardContent></Card></SolicitorPortalShell>}
