/**
 * Browser entry point for the condition-record submission contract.
 *
 * The dialog validates with the same module the edge operation refuses
 * with, so what an operator is asked for and what the server accepts cannot
 * become two standards. See
 * `supabase/functions/_shared/reports/risk/conditionRecordSubmission.pure.ts`.
 */
export * from '../../../supabase/functions/_shared/reports/risk/conditionRecordSubmission.pure.ts';
