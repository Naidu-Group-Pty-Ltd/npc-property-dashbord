/**
 * Bridge — the implementation lives with the Edge Functions.
 *
 * A chosen template, as the design of a document drawn in the browser rather
 * than typeset. Nothing may be added here; see
 * `__tests__/designSystemSourceOfTruth.spec.ts`.
 */
export * from '../../../supabase/functions/_shared/reportDesign/drawnDesign.pure.ts';
