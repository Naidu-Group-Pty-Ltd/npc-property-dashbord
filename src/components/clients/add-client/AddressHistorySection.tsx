import { Controller,useFormContext } from 'react-hook-form'; import { MapPin } from 'lucide-react'; import { FormField } from './FormField'; import { LIVING_SITUATION_OPTIONS } from '@/lib/client-fact-find/fieldDefinitions'; import { Label } from '@/components/ui/label'; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select'; import { AdvancedSectionHeader,premiumSectionClass } from './AdvancedSectionHeader';
export function AddressHistorySection({applicantIndex}:{applicantIndex:0|1}){const {control}=useFormContext();const base=applicantIndex*2;return <section className={premiumSectionClass}><AdvancedSectionHeader icon={MapPin} title={`Applicant ${applicantIndex+1} Address History`} description="Current and previous residential details."/><div className="space-y-5 p-5">{(['Current','Previous'] as const).map((title,offset)=><div key={title} className="grid gap-4 rounded-2xl border border-border/50 bg-background/35 p-4 sm:grid-cols-2"><div className="sm:col-span-2"><FormField name={`addresses.${base+offset}.address`} label={`${title} Address`}/></div><div className="space-y-2"><Label>{title} Living Situation</Label><Controller control={control} name={`addresses.${base+offset}.livingSituation`} render={({field})=><Select value={field.value} onValueChange={field.onChange}><SelectTrigger className="rounded-xl bg-background/70"><SelectValue placeholder="Select situation"/></SelectTrigger><SelectContent>{LIVING_SITUATION_OPTIONS.map(x=><SelectItem value={x} key={x}>{x}</SelectItem>)}</SelectContent></Select>}/></div><FormField name={`addresses.${base+offset}.movedInDate`} label={title==='Current'?'Date Moved In':'Previous Date Moved In'} type="date"/></div>)}</div></section>}
