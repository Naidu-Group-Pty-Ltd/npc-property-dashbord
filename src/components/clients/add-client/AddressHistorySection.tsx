import { Controller,useFormContext } from 'react-hook-form'; import { FormField } from './FormField'; import { LIVING_SITUATION_OPTIONS } from '@/lib/client-fact-find/fieldDefinitions'; import { Label } from '@/components/ui/label'; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
export function AddressHistorySection({applicantIndex}:{applicantIndex:0|1}){const {control}=useFormContext();const base=applicantIndex*2;return <section className="space-y-4 rounded-2xl border border-border bg-card/70 p-4"><h3 className="font-semibold">Applicant {applicantIndex+1} Address History</h3>{(['Current','Previous'] as const).map((title,offset)=><div key={title} className="grid gap-3 border-t border-border/70 pt-3 sm:grid-cols-3"><FormField name={`addresses.${base+offset}.address`} label={`${title} Address`}/><div className="space-y-1.5"><Label>{title} Living Situation</Label><Controller control={control} name={`addresses.${base+offset}.livingSituation`} render={({field})=><Select value={field.value} onValueChange={field.onChange}><SelectTrigger><SelectValue placeholder="Select situation"/></SelectTrigger><SelectContent>{LIVING_SITUATION_OPTIONS.map(x=><SelectItem value={x} key={x}>{x}</SelectItem>)}</SelectContent></Select>}/></div><FormField name={`addresses.${base+offset}.movedInDate`} label={title==='Current'?'Date Moved In':'Previous Date Moved In'} type="date"/></div>)}</section>}
