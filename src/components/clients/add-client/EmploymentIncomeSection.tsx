import { useFormContext,useWatch } from 'react-hook-form'; import { FormField } from './FormField'; import { calculateFactFindTotals,centsToDisplay } from '@/lib/client-fact-find/calculations'; import type { AdvancedClientCreationPayload } from '@/lib/client-fact-find/types';
export function EmploymentIncomeSection({index}:{index:0|1}){const {control,getValues}=useFormContext<AdvancedClientCreationPayload>();useWatch({control,name:`employment.${index}`});const totals=calculateFactFindTotals(getValues());const total=index===0?totals.applicant1AnnualIncomeCents:totals.applicant2AnnualIncomeCents;return <section className="space-y-4 rounded-2xl border border-border bg-card/70 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><h3 className="font-semibold">Applicant {index+1} Employment and Income</h3><div className="rounded-lg bg-brand-300/10 px-3 py-2 text-sm"><span className="text-muted-foreground">Total Annual Income </span><strong>{centsToDisplay(total)}</strong></div></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><FormField name={`employment.${index}.employmentType`} label="Employment Type"/><FormField name={`employment.${index}.employerOrBusiness`} label="Employer / Business"/><FormField name={`employment.${index}.roleOrPosition`} label="Role / Position"/><FormField name={`employment.${index}.employerAddress`} label="Employer Address"/><FormField name={`employment.${index}.startDate`} label="Start Date" type="date"/><FormField name={`employment.${index}.baseSalary`} label="Base Salary — Annual" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.bonus`} label="Bonus" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.commission`} label="Commission" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.overtime`} label="Overtime" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.otherTaxableIncome`} label="Other Taxable Income" type="number" min="0" step="0.01"/></div></section>}
