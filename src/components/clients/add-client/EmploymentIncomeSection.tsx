import { useFormContext,useWatch } from 'react-hook-form'; import { BriefcaseBusiness } from 'lucide-react'; import { FormField } from './FormField'; import { calculateFactFindTotals,centsToDisplay } from '@/lib/client-fact-find/calculations'; import type { AdvancedClientCreationPayload } from '@/lib/client-fact-find/types'; import { AdvancedSectionHeader,premiumSectionClass } from './AdvancedSectionHeader';
export function EmploymentIncomeSection({index}:{index:0|1}){const {control,getValues}=useFormContext<AdvancedClientCreationPayload>();useWatch({control,name:`employment.${index}`});const totals=calculateFactFindTotals(getValues());const total=index===0?totals.applicant1AnnualIncomeCents:totals.applicant2AnnualIncomeCents;const totalChip=<div className="rounded-2xl border border-brand-300/25 bg-brand-300/10 px-4 py-2 text-right"><p className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Annual income</p><strong className="text-sm text-foreground">{centsToDisplay(total)}</strong></div>;return <section className={premiumSectionClass}><AdvancedSectionHeader icon={BriefcaseBusiness} title={`Applicant ${index+1} Employment and Income`} description="Current employment profile and taxable annual income." trailing={totalChip}/><div className="grid gap-x-5 gap-y-4 p-5 sm:grid-cols-2"><FormField name={`employment.${index}.employmentType`} label="Employment Type"/><FormField name={`employment.${index}.employerOrBusiness`} label="Employer / Business"/><FormField name={`employment.${index}.roleOrPosition`} label="Role / Position"/><FormField name={`employment.${index}.employerAddress`} label="Employer Address"/><FormField name={`employment.${index}.startDate`} label="Start Date" type="date"/><FormField name={`employment.${index}.baseSalary`} label="Base Salary — Annual" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.bonus`} label="Bonus" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.commission`} label="Commission" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.overtime`} label="Overtime" type="number" min="0" step="0.01"/><FormField name={`employment.${index}.otherTaxableIncome`} label="Other Taxable Income" type="number" min="0" step="0.01"/></div></section>}
