import { Controller, useFormContext } from 'react-hook-form';
import { FormField } from './FormField'; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select'; import { Label } from '@/components/ui/label'; import { TITLE_OPTIONS } from '@/lib/client-fact-find/fieldDefinitions';
export function ApplicantDetailsSection({index,optional=false}:{index:0|1;optional?:boolean}){const {control}=useFormContext();return <section className="space-y-4 rounded-2xl border border-border bg-card/70 p-4"><h3 className="font-semibold">Applicant {index+1} Personal Details {optional&&<span className="ml-2 text-xs font-normal text-muted-foreground">Optional</span>}</h3><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="space-y-1.5"><Label>Title</Label><Controller control={control} name={`applicants.${index}.title`} render={({field})=><Select value={field.value} onValueChange={field.onChange}><SelectTrigger><SelectValue placeholder="Select title"/></SelectTrigger><SelectContent>{TITLE_OPTIONS.map(x=><SelectItem key={x} value={x}>{x}</SelectItem>)}</SelectContent></Select>}/></div><FormField name={`applicants.${index}.firstName`} label="First Name" required={index===0}/><FormField name={`applicants.${index}.middleName`} label="Middle Name"/><FormField name={`applicants.${index}.surname`} label="Surname" required={index===0}/><FormField name={`applicants.${index}.dateOfBirth`} label="Date of Birth" type="date"/><FormField name={`applicants.${index}.gender`} label="Gender"/><FormField name={`applicants.${index}.maritalStatus`} label="Marital Status"/><FormField name={`applicants.${index}.residencyStatus`} label="Residency Status"/><FormField name={`applicants.${index}.numberOfDependants`} label="Number of Dependants" type="number" min="0"/><FormField name={`applicants.${index}.mobile`} label="Mobile" type="tel"/><FormField name={`applicants.${index}.email`} label="Email" type="email"/></div></section>}
